// const logger = require("../../_utils/logger");
// const nextErr = require("../../_middleware/handerError");
// const { ErrorHandler } = require("../../_middleware/handling/ErrorHandle");
// const { payload } = require("../../_config/validator/payload");
// const responseValidate = require("./response.error");
// const registerValidation = (validationSchema) => {
//   return async (req, res, next) => {
//     const error = validationSchema.validate(payload(req).register, {
//       abortEarly: true,
//     });
//     return responseValidate(error, res, next);
//   };
// };

// const loginValidation = (validationSchema)=>{
  
// }

// module.exports = { registerValidation };
